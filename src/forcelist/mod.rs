//! ExtensionInstallForcelist managed-policy writer/remover (issue #612, WU-3).
//!
//! STUB — implementation lands next (TDD). Force-installs the DIG extension
//! into each user-selected Chromium browser via each browser's managed-policy
//! surface, marker-owned so uninstall removes only DIG entries.
