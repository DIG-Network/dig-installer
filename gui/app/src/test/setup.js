// Vitest global setup: pull in jest-dom's custom matchers (toBeInTheDocument,
// toBeChecked, …) so the component suite reads declaratively. Loaded via
// `setupFiles` in vitest.config.js before every test file.
import "@testing-library/jest-dom/vitest";
